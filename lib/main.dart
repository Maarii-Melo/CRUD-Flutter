import 'package:flutter/material.dart';
import 'package:CRUDPassagaem/listview_passagem.dart';

void main() => runApp(
      MaterialApp(
        title: 'Carregando dados',
        home: ListViewPassagem(),
      ),
    );
